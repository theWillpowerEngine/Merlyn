TODO (docs)
	
TODO (tests)

	new and different CTOR permutations (default, extra parms error, not enough parms nil-autofill)
	
	default parameters
	predicate parameters (kind of already tested because we use them for shit, but let's actually test them)

	inherit (SSL std library)


TODO

	Make .shrp files work with shiro.  Make imports work with project-relative paths if being run in a project context.