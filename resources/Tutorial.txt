TODO (docs)
	
TODO (tests)

	switch
	error?
	new and different CTOR permutations (default, extra parms error, not enough parms nil-autofill)
	and / or
	
	inherit (SSL std library)


TODO

	Make .shrp files work with shiro (and work fully with SENSE).  Make imports work with project-relative paths if being run in a project context.
