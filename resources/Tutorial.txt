TODO (docs)

	default parameters (v=hello:str?)->(...		;note, the predicate is optional just shown so you remember the order


	
TODO (tests)

	switch
	error?
	new and different CTOR permutations (default, extra parms error, not enough parms nil-autofill)
	and / or
	
	default parameters
	predicate parameters (kind of already tested because we use them for shit, but let's actually test them)


	inherit (SSL std library)


TODO

	Make .shrp files work with shiro.  Make imports work with project-relative paths if being run in a project context.

	Inline object reader needs to lift the processing of lists out of the reader and into the parser (see below snippet)
		(do 
			(cls)
			(defn prop (pv cb-or-queue=) ([val $pv]
				(enclose (v=)->( do (
					(if (nil? $v) $val (sod val $v))
				)))
			))
    
			(sod o { name: (prop 'Dan'), age: (prop 36) })
			(print (.c o.name ())))

	Make autolambdas in dot unwinding work with lambdas which can take zero parameters (as o.name in the example above)