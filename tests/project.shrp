(do (import shiro-project)     (sh-install 'test')
    (sh-project test '(
      (shp-file 'test.shr' 'tests.shr')
    (shp-folder 'litterbox' '(
		(shp-file 'server.shr' 'passthru-server.shr')	
    ))

)))